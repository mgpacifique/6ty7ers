# Database Schema — Data Dictionary

PostgreSQL schema for the queue system. The source of truth is
`backend/app/models.py` and the Alembic migration `79dfbd94357c_initial_schema`.
This document describes every table, column type and key, and shows the sample
rows loaded by `backend/app/seed_sample_data.py`.

## Entity overview

```
patients 1 ──── * queue_sessions * ──── 1 staff   (triaged_by / consulted_by)
                      │
                      └── events recorded in system_logs
```

## Table: patients

Stores private patient identity. Never shown on public screens — public
displays only ever use `queue_sessions.public_token`.

| Column | Type | Key / Constraint | Notes |
|---|---|---|---|
| id | UUID | PK | generated by the app |
| full_name | VARCHAR | NOT NULL | private (PII) |
| phone_number | VARCHAR | NOT NULL | for notifications |
| created_at | TIMESTAMPTZ | | set on insert (UTC) |

## Table: staff

| Column | Type | Key / Constraint | Notes |
|---|---|---|---|
| id | UUID | PK | |
| username | VARCHAR | UNIQUE, NOT NULL, indexed | login handle |
| password_hash | VARCHAR | NOT NULL | bcrypt hash, never plain text |
| role | VARCHAR | NOT NULL | `Admin`, `Nurse` or `Doctor` |
| created_at | TIMESTAMPTZ | | |

## Table: queue_sessions

One row per patient visit. This is the queue itself.

| Column | Type | Key / Constraint | Notes |
|---|---|---|---|
| id | UUID | PK | |
| patient_id | UUID | FK → patients.id, NOT NULL | |
| public_token | VARCHAR | UNIQUE, NOT NULL, indexed | `EM-###` urgent, `FT-###` routine |
| track_type | VARCHAR | nullable | `Urgent` or `Routine`, set at triage |
| priority_score | INTEGER | default 0 | calculated by the Smart Logic Engine |
| status | VARCHAR | | `Registered` → `Triaged` → `Waiting` → `Called` → `Completed` |
| t1_check_in | TIMESTAMPTZ | | T1: arrival time |
| t2_called | TIMESTAMPTZ | nullable | T2: called to room |
| t3_completed | TIMESTAMPTZ | nullable | T3: session end |
| triaged_by_staff_id | UUID | FK → staff.id, nullable | nurse who triaged |
| consulted_by_staff_id | UUID | FK → staff.id, nullable | doctor who consulted |

KPI: **true wait time = t2_called − t1_check_in**, straight from the columns.

## Table: system_logs

| Column | Type | Key / Constraint | Notes |
|---|---|---|---|
| id | UUID | PK | |
| event_type | VARCHAR | NOT NULL, indexed | e.g. `EMERGENCY_INSERTION` |
| description | VARCHAR | NOT NULL | human-readable detail |
| timestamp | TIMESTAMPTZ | | |

## Sample data rows

Loaded by running (from `backend/`, with `DATABASE_URL` set):

```bash
python -m app.seed_sample_data
```

The script is safe to re-run — it removes its own rows first, and touches
nothing else.

### staff (3 rows)

| username | role | password (sample only) |
|---|---|---|
| admin_amina | Admin | admin123 |
| nurse_grace | Nurse | nurse123 |
| doctor_jean | Doctor | doctor123 |

Passwords are stored bcrypt-hashed; the plain values above exist only for
local testing.

### patients (5 rows)

| full_name | phone_number |
|---|---|
| Aline Uwimana | +250781000001 |
| Eric Niyonzima | +250781000002 |
| Claudine Mukamana | +250781000003 |
| Samuel Habumugisha | +250781000004 |
| Josiane Ingabire | +250781000005 |

### queue_sessions (6 rows)

Covers both tracks, every status in the lifecycle, and all three FKs.

| public_token | track_type | status | priority | T1 | T2 | T3 | triaged_by | consulted_by |
|---|---|---|---|---|---|---|---|---|
| EM-101 | Urgent | Waiting | 100 | −40 min | | | nurse_grace | |
| EM-102 | Urgent | Called | 100 | −25 min | −5 min | | nurse_grace | doctor_jean |
| FT-201 | Routine | Waiting | 10 | −30 min | | | nurse_grace | |
| FT-202 | Routine | Triaged | 10 | −20 min | | | nurse_grace | |
| FT-203 | Routine | Completed | 10 | −90 min | −66 min | −50 min | nurse_grace | doctor_jean |
| FT-204 | | Registered | 0 | −2 min | | | | |

Times are offsets from the moment the seed runs. FT-203 demonstrates the KPI:
true wait = T2 − T1 = 24 minutes.

### system_logs (3 rows)

| event_type | description |
|---|---|
| PATIENT_CHECK_IN | Patient checked in with token FT-204 |
| PATIENT_TRIAGED | Session FT-202 triaged as Routine with score 10 |
| EMERGENCY_INSERTION | Session EM-101 placed at the head of the Urgent track |
